{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "git",
            "type": "shell",
            "command": "git add -A; git commit --quiet --allow-empty-message --message ''; git push --quiet;"
        }
    ]
}